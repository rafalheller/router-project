import React, {Component} from 'react';
import {Route} from 'react-router-dom'

class Page extends Component {
    render() {
        return (
            <Route/>
        );
    }
}

export default Page;